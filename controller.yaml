removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: dan-cloudbees
  groups:
  - members:
      users:
      - dromano13
      - dromano13-admin
    roles:
    - name: browse
      grantedAt: current
    - name: workshop-admin
      grantedAt: child
    name: Team Administrators
  filteredRoles:
  - workshop-admin
  - browse
  items:
  - kind: managedController
    name: dev-controller
    properties:
    - healthReporting:
        enabled: true
    - configurationAsCode:
        bundle: dan-cloudbees-dev-controller
    - optOutProperty:
        securityEnforcerOptOutMode:
          authorizationOptOutMode: {
            }
    configuration:
      kubernetes:
        memory: 4000
        cpus: 1.0
        clusterEndpointId: default
        disk: 10
        storageClassName: premium-rwo
        domain: dan-cloudbees-dev-controller
        namespace: controllers
        yaml: |
          kind: "StatefulSet"
          spec:
            template:
              metadata:
                labels:
                  networking/allow-internet-access: "true"
              spec:
                containers:
                - name: "jenkins"
                  env:
                  - name: "SECRETS"
                    value: "/var/jenkins_home/jcasc_secrets"
                  - name: "GITHUB_ORGANIZATION"
                    value: "dan-cloudbees"
                  - name: "GITHUB_USER"
                    value: "dromano13"
                  - name: "GITHUB_APP"
                    value: "cloudbees-ci-casc-workshop"
                  - name: "CONTROLLER_SUBDOMAIN"
                    value: "dan-cloudbees-dev-controller"
                  - name: "CASC_BUNDLE_ID"
                    value: "dan-cloudbees-dev-controller"
                  volumeMounts:
                  - name: "jcasc-secrets"
                    mountPath: "/var/jenkins_home/jcasc_secrets"
                volumes:
                - name: "jcasc-secrets"
                  secret:
                    secretName: cbci-mc-secret
